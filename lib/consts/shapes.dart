import 'package:dzien_dobry/consts/paddings.dart';
import 'package:flutter/material.dart';

class MyRadius {
  static BorderRadius small = BorderRadius.circular(MyPaddings.small);
  static BorderRadius medium = BorderRadius.circular(MyPaddings.medium);
  static BorderRadius large = BorderRadius.circular(MyPaddings.large);
}
